import * as React from 'react' // Flow recommended this
import H1 from '../headers/h1'

const Loading = () => {
    return (
        <H1>
            Either the page is loading... Or No Recipe currently available, try
            later :)
        </H1>
    )
}

export default Loading
